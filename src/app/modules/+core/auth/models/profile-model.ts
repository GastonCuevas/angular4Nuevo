export interface ProfileModel {
    aud: string | null,
    exp: number | null,
    given_name: string | null,
    iat: number | null,
    iss: string | null,
    jti: string | null,
    nameid: number | null,
    nfb: number | null,
    sub: string | null,
}
